# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
edition = "2018"
name = "eos-s3-pac"
version = "0.1.0"
authors = ["Martin Atkins <mart@degeneration.co.uk>"]
exclude = ["/svd/"]
description = "Low-level register mappings for the ARM Cortex-M4 core in the QuickLogic EOS S3"
license = "MIT"
repository = "https://github.com/apparentlymart/rust-eos-s3-pac"

[lib]
path = "src/lib.rs"

[package.metadata.release]
disable-publish = true
disable-push = true
pre-release-commit-message = "cargo: eos-s3-pac release {{version}}"
pro-release-commit-message = "cargo: development version bump"
sign-commit = false
tag-message = "eos-s3-pac {{version}}"
upload-doc = false

[dependencies]
bare-metal = "0.2.0"
cortex-m = "0.5.8"
vcell = "0.1.0"

[dependencies.cortex-m-rt]
optional = true
version = "0.6.5"

[features]
rt = ["cortex-m-rt/device"]
